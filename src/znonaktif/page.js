// export { default } from "next-auth/middleware";

// export const config = {
//     matcher: ["/", "/akun", "/product", "/history"],
// };

function middleware() {
  return <div>middleware</div>;
}

export default middleware;
